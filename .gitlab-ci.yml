include:
  - project: "infra154/ci-cd-includes"
    file: "functions/tf_oracle.yml"

0-prod-platform:
  extends: .plan
  variables:
    ENV: prod
    DIR: platform

1-prod-platform:
  extends: .apply
  variables:
    ENV: prod
    DIR: platform

0-prod-resources:
  extends: .plan
  variables:
    ENV: prod
    DIR: resources

1-prod-resources:
  extends: .apply
  variables:
    ENV: prod
    DIR: resources
